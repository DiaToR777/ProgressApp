<UserControl x:Class="ProgressApp.WpfUI.Views.Analytics.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgressApp.WpfUI.Views.Analytics"
             mc:Ignorable="d" 
          
             xmlns:vmHeatmap="clr-namespace:ProgressApp.WpfUI.ViewModels.Analytics.Heatmap"
             xmlns:vmTable="clr-namespace:ProgressApp.WpfUI.ViewModels.Analytics.Table"
             
             xmlns:viewHeatmap="clr-namespace:ProgressApp.WpfUI.Views.Analytics.Heatmap"
             xmlns:viewTable="clr-namespace:ProgressApp.WpfUI.Views.Analytics.Table"
             
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vmHeatmap:HeatmapViewModel}">
            <viewHeatmap:HeatmapView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vmTable:TableViewModel}">
            <viewTable:TableView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15">
            <TextBlock Text="Режим просмотра:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Gray"/>

            <ComboBox ItemsSource="{Binding ViewOptions}"
          SelectedValue="{Binding SelectedViewOption}"
          SelectedValuePath="Value" 
          Width="200">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayName}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
        </StackPanel>

        <ContentControl Grid.Row="1" 
                        Content="{Binding CurrentAnalyticsView}" 
                        Margin="15,0,15,15"/>
    </Grid>
</UserControl>
