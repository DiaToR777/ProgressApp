<UserControl x:Class="ProgressApp.WpfUI.Views.Analytics.Heatmap.HeatmapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:heatmapConv="clr-namespace:ProgressApp.WpfUI.Converters.Heatmap"
             xmlns:conv="clr-namespace:ProgressApp.WpfUI.Converters"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:helpers="clr-namespace:ProgressApp.WpfUI.Localization.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:IntToThicknessConverter x:Key="IntToThickness"/>
        <heatmapConv:DayResultToColorConverter x:Key="ResultToColor"/>
    </UserControl.Resources>

    <Grid Margin="10" helpers:LanguageHelper.UpdateLanguage="{Binding Source={StaticResource Langs}, Path=CurrentCulture}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0" ItemsSource="{Binding Ranges}"
                     SelectedValue="{Binding SelectedRange}"
                     SelectedValuePath="Value" BorderThickness="0" Background="Transparent" Margin="0,0,0,12">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,4"/>
                        <Setter Property="Margin" Value="2,0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" 
            CornerRadius="4" 
            Padding="{TemplateBinding Padding}">
                                        <TextBlock Text="{Binding DisplayName}" FontSize="12"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12"
                        Visibility="{Binding ShowNavigation, Converter={StaticResource BoolToVis}}">
                <ui:Button Command="{Binding PreviousPeriodCommand}" Icon="{ui:SymbolIcon Symbol=ArrowLeft12}"/>
                <TextBlock Text="{Binding PeriodTitle}" FontSize="13" FontWeight="Medium" VerticalAlignment="Center" Margin="8,0"/>
                <ui:Button Command="{Binding NextPeriodCommand}" Icon="{ui:SymbolIcon Symbol=ArrowRight12}"/>
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ItemsControl Grid.Column="0" ItemsSource="{Binding DayLabels}" Margin="0,0,6,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Width="25" FontSize="11" Foreground="Gray" 
                                       TextAlignment="Right" VerticalAlignment="Center"
                                       Height="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       Margin="{Binding DataContext.CellMargin, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource IntToThickness}}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Weeks}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Height="{Binding DataContext.CellSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Margin="{Binding DataContext.CellMargin, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource IntToThickness}}"
                                                    CornerRadius="4" Cursor="Hand"
                                                    Background="{Binding Result, Converter={StaticResource ResultToColor}}">
                                                <Border.ToolTip>
                                                    <ToolTip>
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Date, StringFormat='{}{0:dd MMMM yyyy}'}" FontWeight="Medium"/>
                                                            <TextBlock Text="{Binding Result, Converter={StaticResource EnumConverter}}" Foreground="Gray" FontSize="12"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Border.ToolTip>
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Opacity" Value="0.75"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Border.InputBindings>
                                                    <MouseBinding MouseAction="LeftClick" 
                                                                  Command="{Binding DataContext.CellClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  CommandParameter="{Binding}"/>
                                                </Border.InputBindings>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>

        <StackPanel Grid.Column="1" Margin="20,0,0,0">
            <ui:TextBlock Text="{Binding Source={StaticResource Langs}, Path=[Title_DayDetails]}" 
                          FontWeight="Bold" FontSize="18" Margin="0,0,0,12"/>
            <ui:Card Padding="20">
                <Grid>
                    <TextBlock Text="{Binding Source={StaticResource Langs}, Path=[Analytics_Heatmap_HintSelectCell]}" Foreground="Gray" FontSize="13" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedCell}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedCell}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <ui:TextBlock Appearance="Secondary" Text="{Binding SelectedCell.Date, StringFormat='{}{0:dd MMMM yyyy, dddd}'}"/>
                        <ui:TextBlock FontSize="15" FontWeight="Medium" Margin="0,8,0,0" Text="{Binding SelectedCell.Result, Converter={StaticResource EnumConverter}}"/>
                        <ui:TextBlock Appearance="Secondary" FontSize="12" Margin="0,10,0,0" Text="{Binding Source={StaticResource Langs}, Path=[Title_Description]}"/>
                        <ScrollViewer Height="150" Margin="0,5,0,0" VerticalScrollBarVisibility="Auto">
                            <ui:TextBlock Text="{Binding SelectedCell.Description}" TextWrapping="Wrap" FontSize="13"/>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </ui:Card>
        </StackPanel>
    </Grid>
</UserControl>