<UserControl x:Class="ProgressApp.WpfUI.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgressApp.WpfUI.Views.Login"
             xmlns:vm="clr-namespace:ProgressApp.WpfUI.ViewModels.Login"
             xmlns:loc="clr-namespace:ProgressApp.WpfUI.Localization.Helpers"
             xmlns:helpers="clr-namespace:ProgressApp.WpfUI.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">

        <Label Content="{Binding Source={StaticResource Langs}, Path=[Title_Welcome]}"
               FontSize="22" HorizontalAlignment="Center"/>

        <TextBlock Text="Pass:" FontSize="22"  HorizontalAlignment="Center"/>

        <PasswordBox x:Name="PassBox"
                     helpers:PasswordHelper.Attach="True"
                     helpers:PasswordHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Height="30"
                     FontSize="24"
                     TabIndex="0"
                     MaxLength="8"/>

        <Button Content="Login" 
            Command="{Binding LoginCommand}" 
            IsDefault="True" 
            Height="35"
            Margin="0,30,0,0"
                />

    </StackPanel>

</UserControl>
