<Window x:Class="ProgressApp.MainWindow"
        xmlns:conv="clr-namespace:ProgressApp.Converters"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ProgressApp"
        Title="ProgressApp"
        Height="600"
        Width="900"
        Background="{DynamicResource WindowBackground}">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Левая панель -->
            <ColumnDefinition Width="160"/>
            <!-- Контент -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Навигация -->
        <StackPanel Grid.Column="0" Background="{DynamicResource ControlBackground}"
                    Visibility="{Binding IsNavigationVisible,
                                         Converter={StaticResource BoolToVisibilityConverter}}">

            <Button Content="Сегодня"
                    Margin="10"
                    Command="{Binding ShowTodayCommand}"
                    />

            <Button Content="Таблица"
                    Margin="10"
                    Command="{Binding ShowTableCommand}"
                    />

            <Button Content="Настройки"
                    Margin="10"
                    Command="{Binding ShowSettingsCommand}"
                    />
        </StackPanel>

        <!-- Контент -->
        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentView}"
                        Margin="10"/>
    </Grid>
</Window>
